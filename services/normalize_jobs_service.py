from typing import List

from models.friendly_job import FriendlyJob
from models.normalized_job import NormalizedJob


def _normalize_seniority(job: FriendlyJob) -> str:
    return

def _normalize_location(job: FriendlyJob) -> str:
    return

def _normalize_field(job: FriendlyJob) -> str:
    return

def normalize_jobs(jobs: List[FriendlyJob]) -> List[NormalizedJob]:
    return